apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "voiceagent.fullname" . }}-agents
  labels:
    {{- include "voiceagent.labels" . | nindent 4 }}
data:
  agents.yaml: |
    {{- toYaml .Values.agents | nindent 4 }}
