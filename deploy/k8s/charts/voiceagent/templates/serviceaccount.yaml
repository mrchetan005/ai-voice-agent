{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "voiceagent.serviceAccountName" . }}
  labels:
    {{- include "voiceagent.labels" . | nindent 4 }}
{{- end }}
