"""LiveKit integration layer — the ONLY package that imports ``livekit*``.

Everything here translates between the engine-neutral core (voiceagent.*)
and livekit-agents / livekit-api / livekit-rtc.
"""

from __future__ import annotations
